<UserControl x:Class="CocktailizrClient.View.Cocktail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1280" d:DesignWidth="1024"
             DataContext="{Binding Cocktail, Source={StaticResource Locator}}"
             Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
    <UserControl.Resources>
        <ResourceDictionary Source="../Themes/Generic.xaml"></ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/beach.jpg" Opacity="0.3"/>
        </Grid.Background>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"></ColumnDefinition>
                <ColumnDefinition Width="1*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Button Command="{Binding BackToSearchClickedCommand}" Width="Auto" Margin="5" HorizontalAlignment="Left" Style="{DynamicResource MaterialDesignFloatingActionButton}">
                    <Image Source="/Assets/ic_action_back.png"></Image>
                </Button>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Margin="5" Style="{DynamicResource MaterialDesignFloatingActionButton}">
                        <Image Source="/Assets/ic_action_back.png"></Image>
                    </Button>
                    <Label HorizontalAlignment="Center" Content="{Binding ShownCocktail.Name}" FontSize="{StaticResource HeadingFontSize}"></Label>
                    <Button Margin="5" Style="{DynamicResource MaterialDesignFloatingActionButton}">
                        <Image Source="/Assets/ic_action_forward.png"></Image>
                    </Button>
                </StackPanel>
                <Image Source="/Assets/wmojtobasse.png" HorizontalAlignment="Center" Height="500"></Image>

                <Label Content="Zubereitung:"></Label>
                <Label Content="Binding für den Zubereirungs-Text blablablabla"></Label>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <Label Content="Benötigte Zutaten:"></Label>
                <ItemsControl ItemsSource="{Binding ShownCocktail.Zutaten}" Margin="5">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock>
                                     <Run Text="{Binding Path=Value, Mode=OneWay}" />
                                     <Run Text="{Binding Path=Key.Skala}" />
                                     <Run Text="{Binding Path=Key.Name}" />
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

        </Grid>

    </Grid>
</UserControl>
